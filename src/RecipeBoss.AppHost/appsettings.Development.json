{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_cosmosNote": "Local dev always uses the Cosmos vnext-preview emulator (ARM64-compatible). For production Cosmos DB, configure the connection string via Azure deployment settings or Aspire publish manifest — do not set it here."
}
